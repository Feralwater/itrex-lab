export type Options = {
    value?: string
    label: string
}

export type CustomSelectPropsType = {
    valuesForSelect: { selectedValue: string, doctorID: string }[]
    placeholder: string
    id: string
    labelText: string
}
