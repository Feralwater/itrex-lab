export interface AppointmentCardProps {
  specialization?: string;
  firstName: string;
  lastName: string;
  avatar: string;
  status: string;
  time: string;
  reason: string;
  note: string;
  role: string;
}
