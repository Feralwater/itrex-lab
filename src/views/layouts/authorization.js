const Authorization = {
    render: async (main) => {
        return `
         <main class="body-auth">${main}</main>
        `
    },
    afterRender: async () => {
    }

}

export default Authorization;